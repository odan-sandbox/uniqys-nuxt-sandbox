{
  "name": "uniqys-nuxt-sandbox",
  "private": true,
  "workspaces": ["frontend", "backend", "aws"],
  "devDependencies": {
  },
  "scripts": {
    "build": "yarn workspace frontend build",
    "start": "yarn workspace backend start"
  }
}
